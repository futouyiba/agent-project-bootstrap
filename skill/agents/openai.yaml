interface:
  display_name: "Agent 项目工作流"
  short_description: "初始化仓库并用自然语言管理 GitHub 日常交付"
  default_prompt: "Use $agent-project-bootstrap to initialize this repository or operate its daily GitHub Issue, Project, PR, and CI workflow from my natural-language request."
policy:
  allow_implicit_invocation: true
